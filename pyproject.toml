[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line too long — handled by notebooks differently
    "F401",  # unused imports — common in notebooks
    "E402",  # module import not at top — common in notebooks
]
